import { Story } from '@storybook/addon-docs';

# Videos

There's a video-embed plugin installed on RS Drupal, that makes iframe videos responsive automatically
+ base js and normalize.scss do the same for other iframes
