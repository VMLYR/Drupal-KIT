import { Story } from '@storybook/addon-docs';

# Lists

3 types of lists used:
- unordered
  - special variant = 'list--item', this one is used in search results, for example
- ordered
- definition lists

## Styling

Pay special attention to the `text-long` styling, which produces the styling for text coming from CKEditor.<br />
