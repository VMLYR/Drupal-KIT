import { Story } from '@storybook/addon-docs';

# USP

A custom block type, used to add USP-items on the homepage.<br />
Advantage to using this instead of a block with the USP paragraph, is that we don't have to override anything.
