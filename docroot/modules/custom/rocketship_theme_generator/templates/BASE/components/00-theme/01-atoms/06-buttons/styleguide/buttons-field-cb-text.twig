{#
/**
 * This is strictly an example file to demonstrate markup in Storybook.
 * It is not intended to be included in any other component.
 */
#}

{#
  We're flipping things upside down a little bit, by embedding a dummy Content Block component in here and only rendering the buttons field
  That way, we can show the Content Block Buttons in combination with the background-color variations
#}
{% include "@%theme_prefix%-cb-molecules/001-text/styleguide/text.twig" with {
    label: variant.label,
    configuration: variant.configuration,
    block_type: variant.block_type,
    bem_block_modifiers: variant.bem_block_modifiers,
    bem_block_extra: variant.bem_block_extra,
    fields: variant.fields,
  }
%}
